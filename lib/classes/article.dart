class Articles {
  String author;
  String title;
  String content;
  String url;
  String urlToImage;

  Articles(
      {
        required this.author,
        required this.title,
        required this.content,
        required this.url,
        required this.urlToImage
      }
  );
}